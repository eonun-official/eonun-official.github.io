# 网站完全指南（小白版）

> 写给完全不懂 Hugo 的自己。读完这一份，就能搞懂文件结构、启动网站、日常修改。
> 变更历史见文末附录。

---

## 一、先搞懂最重要的事：content 和 public 到底啥区别？

这两个文件夹名字像、内容也像，但**一个是"图纸"，一个是"盖好的房子"**。

| | `content/`（图纸） | `public/`（盖好的房子） |
| --- | --- | --- |
| 里面是什么 | 6 个页面的**正文**（文字、图片引用、动画代码） | **完整可打开的网页**（浏览器访问的就是它） |
| 能不能直接打开看 | 不能（没有网页头，打开是源码） | 能（双击 index.html 就能看） |
| 珍贵程度 | ★★★★★ **唯一要珍惜的东西** | ★ 随时可以删掉重新生成 |
| 改哪个 | **永远只改这里** | **永远不要手改** |

### 为什么会有"重复"？价值在哪？

`public/` 不是手工复制出来的第二份，而是跑一下 `build.bat`，Hugo **自动**把下面三样东西组装出来的：

```
content/ 的正文  +  layouts/ 的网页壳  +  static/ 的素材（图片/字体/视频/标语）
        ↓  （双击 build.bat）
      public/ 成品网站
```

这个"重复"的价值：

1. **改一处，全站生效**——想改"关于我"页面的文字，只改 `content/about.md`，重新构建，public 里的成品自动更新；
2. **成品随时能重生**——public 删光了都不怕，跑一遍 build.bat 原样回来。所以它里面的文件**不用备份、不用心疼**；
3. **以前的问题就出在这里**：过去让 AI 直接改 public 里的 HTML，等于"跳过图纸改房子"——房子越改越乱，图纸还是旧的，最后两边对不上。现在规矩只有一条：**一切修改走 content，public 只是产物**。

一句话记住：**content 是源（source），public 是影印件（output）。**

---

## 二、每个文件夹是干什么的（逐一点名）

```
my-website/
│
├─ content/          【图纸】6 个页面的正文，改内容就改这里
│   ├─ _index.md         主页（视频背景 + 随机标语 + logo 动效）
│   ├─ about.md          关于我（履历时间线）
│   ├─ contact.md        联系我
│   ├─ shop.md           商店（产品暂隐藏，显示"尽情期待"）
│   ├─ music-services.md 音乐服务（制作、混音母带、流程）
│   └─ instructions.md   使用说明（一对一指导、课程体系）
│
├─ layouts/          【网页壳】每个页面共用的"头部和骨架"
│   ├─ partials/head.html    统一的 <head>：标题、CSS、字体、favicon
│   ├─ _default/baseof.html  页面骨架（body 标签从这来）
│   ├─ index.html / single.html  把正文塞进骨架的装配器
│   └─ 404.html + partials/404-body.html  404 吐槽页
│
├─ static/           【素材仓库】构建时原样拷进 public
│   ├─ css/              全站样式（main.min.css = 配色/字体/排版/动画）+ 两个页面专属样式
│   ├─ fonts/            全部字体（EDIX 像素字体、Alimama 标题字体）
│   ├─ images/           所有照片和图片
│   ├─ videos/           视频（主页背景 THEME.mp4）
│   └─ data/             标语库 trance_mixing_slogans.txt（303 条，想加就加一行）
│
├─ public/           【成品】构建产物 = 双击 start.html 访问的网站
│
├─ tools/            【工具箱】
│   ├─ hugo/hugo.exe     Hugo 程序本体（免安装，0.166.0）
│   └─ scripts/          维护脚本（audit_refs.py 全站资源体检等）
│
├─ backups/          【杂物间】历史压缩包，130MB，确认没用可整个删
│
├─ hugo.toml         【配置文件】网站标题、菜单、域名
├─ build.bat         【出成品】双击 = 重新生成 public（发布前用）
└─ start-server.bat  【开网站】双击 = 启动网络服务器（日常用这个看网站）
    start-server.ps1 / .vbs   备用静态服务器（不依赖 Hugo）
    start.html                手动入口（双击点按钮看页面，最简单）
```

### 每个页面对应关系速查

| 你看到的页面 | 图纸（改这里） | 成品（别动） |
| --- | --- | --- |
| 主页 | `content/_index.md` | `public/index.html` |
| 关于我 | `content/about.md` | `public/about/index.html` |
| 联系我 | `content/contact.md` | `public/contact/index.html` |
| 商店 | `content/shop.md` | `public/shop/index.html` |
| 音乐服务 | `content/music-services.md` | `public/music-services/index.html` |
| 使用说明 | `content/instructions.md` | `public/instructions/index.html` |
| 404 页 | `layouts/404.html` | `public/404.html` |

---

## 三、启动网站（两种方式的区别 + 操作步骤）

### 两种方式调用的是**同一份文件**，区别只在"浏览器怎么读文件"

| | 双击 start.html（手动） | start-server.bat（网络服务器） |
| --- | --- | --- |
| 原理 | 浏览器直接读硬盘文件（file://） | 电脑先起一个服务器，浏览器走 http:// 访问 |
| 页面/图片/字体 | ✅ 正常 | ✅ 正常 |
| **标语随机轮播** | ❌ 只显示固定一句 | ✅ 303 条真随机 |

**标语机制**：主页的 JS 用 `fetch()` 从 `data/trance_mixing_slogans.txt` 读 303 条标语随机展示。浏览器**禁止 file:// 页面 fetch 本地文件**（安全策略），所以双击打开时只能显示兜底标语；走服务器就正常。

**注意**：这个"网"是你电脑自己（localhost），不需要互联网；手机访问走的是家里路由器局域网。

### 操作步骤（网络服务器模式）

1. 双击 `start-server.bat`；
2. 第一次运行 Windows 防火墙会弹窗 → 点 **允许**（勾不勾"专用网络"都行）；
3. 看黑色窗口里显示的地址：
   - **本机看**：浏览器打开 `http://localhost:1313`
   - **手机/平板看**（连同一个 WiFi）：打开 `http://192.168.0.5:1313`（脚本会自动显示本机 IP，以窗口里为准）
4. 改内容：用记事本/任何编辑器改 `content/` 里的文件，**保存**，浏览器里的页面自动刷新；
5. 停止：在黑色窗口里按 `Ctrl+C`，或直接关掉窗口。

### 日常改东西速查表

| 想干嘛 | 动哪里 |
| --- | --- |
| 改任何页面的文字 | `content/` 对应文件 |
| 加一条随机标语 | `static/data/trance_mixing_slogans.txt` 末尾加一行 |
| 换图/换视频 | 把新文件放进 `static/images/` 或 `static/videos/`，文件名保持一致 |
| 发布到公网 | 改 `hugo.toml` 的 baseURL → 双击 `build.bat` → 上传 `public/` |

---

## 附录：这两天的变更记录（摘要）

**2026-09-23 深夜（第 1 轮：清理）**
- 排查并修复全站资源引用：删除残留开发脚本、修复 404 页绝对路径；
- 目录去编号：`2-music-services`→`music-services`、`4-instructions`→`instructions`；
- 清理 7 个死文件（旧版 CSS、无用字体图片），归档进 `backups/2026-09-23-cleanup/`；
- 合并 4 个功能重复的启动脚本为 3 个；重写 README；更新维护日志。

**2026-09-24 凌晨（第 2 轮：Hugo 重建）**
- 从成品反向重建 Hugo 源码层（content / layouts / static），网站重新可以用 Hugo 构建；
- 部署 Hugo 0.166 免安装程序到 `tools/hugo/`；
- `start-server.bat` 重写为局域网服务器（本机 + 手机都能访问）；
- 新增 `build.bat` 一键构建正式版 + `make_relative.py` 相对路径后处理（双击 HTML 也能正常显示）。

**2026-09-24（第 3 轮：事故修复）**
- 事故：资源排查漏扫 CSS 内部引用，误删 EDIX 字体和商店背景图 → 网站显示受损；
- 修复：文件全部恢复；排查脚本升级（新增 CSS 引用扫描）；CSS 内字体路径改相对（双击模式字体也能加载）；
- 教训已固化：删任何文件前必须 HTML + CSS 双重排查。

**2026-09-24（第 4 轮：历史遗留清零）**
- `fronts/`（拼错的字体目录）并入 `fonts/`；
- `ananke/css/` 上移到 `css/`，ananke 骨架删除；
- `THEME.mp4` 从 images 挪到 `videos/`；
- 全程零事故，三道验证链（全站排查 + HTTP 全资源测试 + 原目录冒烟测试）。

**2026-09-24（第 5 轮：上线 + 双托管）**
- 全站加载优化：图片 17MB→0.6MB（webp）、视频 10.3MB→2.2MB、EDIX 字体优先加载、视频后台懒加载；
- 成功上线双托管：GitHub Pages（eonun-official.github.io）+ Cloudflare（eonun.eonun-official.workers.dev），
  以后用 tools\GitPush.hta 点紫色按钮，两边自动一起更新；
- Cloudflare 完整搭建过程和踩坑记录见 project-maintenance-log.md 第 12 节。

**2026-09-24（第 6 轮：唯一托管 + 发布工具升级）**
- 定型 GitHub Pages 为唯一托管（Cloudflare workers.dev 国内被墙，仅备用）；
- GitPush.hta 升级：左栏本地树形目录（文件夹可展开/收起、带勾选）+ 右栏线上仓库实况，
  每次打开默认勾选 = 固化的 39 个约定文件 + 已在仓库的文件；
  取消勾选并推送 = 从 GitHub 移除该文件（本地保留），推送前实时预览、命令全透明；
  详见 project-maintenance-log.md 第 14 节。

**下一步（待办）**：GitHub 仓库转私有（源码保密）→ 绑自定义域名（国内加速）。
