# 项目文档导航

> 本文件原名 `audit-log-2026-09-23.md`（2026-09-23 首次巡检日志）。
> 自 2026-09-24 起，项目只保留**一份持续更新的日志**：`project-maintenance-log.md`，
> 不再按日期新建日志文件。本文件留作首次巡检的历史档案，内容不再更新。

---

# 网站结构与巡检日志（AI 巡检版）

- **巡检日期**：2026-09-23
- **巡检对象**：`my-website`（Eonun 个人网站 / Trance 音乐主题）
- **巡检方式**：全量目录梳理 + 关键文件逐一阅读
- **结论速览**：网站目前是一个「以构建产物为核心」的项目。手动启动链路完整可用；Hugo 链路当前**不具备重新构建出现有网站的能力**，正式上线前必须补齐源码层。

---

## 1. 一句话概括这个项目

这是一个用 **Hugo（静态网站生成器）+ Ananke 主题** 搭建的个人网站，主题是 Trance 音乐制作（自我介绍、音乐服务、使用说明、联系、商店五个页面）。项目的实际形态是：

```
Hugo 源码层（残缺）  →  hugo build  →  public/（完整、可直接部署）
```

**当前唯一完整、可用的部分是 `public/` 目录**——它是 Hugo 构建出来的静态产物，后来又被手工改成全相对路径，因此可以脱离 Hugo 独立运行（双击或任意静态服务器）。

---

## 2. 根目录文件清单与用途

| 文件 / 目录 | 作用 | 备注 |
| --- | --- | --- |
| `public/` | **网站本体**：Hugo 构建产物，可直接部署到任意静态托管 | 约 37 MB，含全部页面、图片、视频、字体、数据文件 |
| `hugo.toml` | Hugo 配置文件：站点标题、主题、导航菜单 | 见 §5 问题清单 |
| `themes/ananke/` | Ananke 主题目录 | **只有空的 `layouts/` 文件夹，主题文件已丢失** |
| `README.md` | 本地启动指南（面向"怎么打开网站"） | 描述的是 public 手动启动方式 |
| `project-maintenance-log.md` | 人工维护日志（截至 2026-02-07） | 记录了大量 public 内 HTML 的手工改动 |
| `start.html` | 网站启动器：一个纯 HTML 的导航页，双击后通过按钮跳转各页面 | 手动方式一的入口 |
| `start-server.bat` | 检测 Hugo 并执行 `hugo server --disableLiveReload`（端口 1313） | **Hugo 方式的入口，但依赖 Hugo 已安装** |
| `start-server.ps1` | 用 PowerShell `HttpListener` 直接伺服 `public/` 目录（端口 8000） | 不依赖 Hugo，推荐的手动方式 |
| `server.ps1` | 与上一文件几乎相同的 HttpListener 服务器（端口 8000），但 public 路径**硬编码为 `c:\Users\Eonun\my-website\public`** | 被 start-server.vbs 调用 |
| `start-server.vbs` | VBS 弹窗提示 + 调用 `server.ps1` | 双击隐藏启动窗口 |
| `my-website.zip` / `my-website 1.0.zip` | 项目压缩备份（各约 37 MB） | 内容大概率重复 |
| `.trae.zip` | Trae 编辑器导出的项目备份（约 27 MB，80 个条目） | 内含一份 `public/` 副本和**空的** `archetypes/`、`data/`、`i18n/`、`layouts/` 目录 |
| `私人非公开程序/` | 空目录 | 仅占位 |
| `.hugo_build.lock` | Hugo 构建锁文件 | 残留物，可删 |

---

## 3. public/ 目录结构（网站本体）

```
public/
├── index.html              # 主页：THEME.mp4 视频背景 + 随机标语轮播 + eonun logo 流光动效
├── about/                  # 关于我：个人履历时间线，深紫磨砂玻璃风格
├── shop/                   # 商店：Trance 采样包（产品已用 display:none 隐藏，显示"尽情期待"）
├── 2-music-services/       # 音乐服务页
├── 4-instructions/         # 使用说明页
├── contact/                # 联系页
├── 404.html                # 404 页
├── categories/  tags/      # Hugo 自动生成的分类 / 标签页
├── index.xml  sitemap.xml  # RSS 订阅 / 网站地图
├── ananke/                 # 主题编译出的 CSS（main.min.css 等）
├── css/                    # 自定义样式：course-page.css、shop-page.css
├── js/                     # 空目录
├── data/
│   └── trance_mixing_slogans.txt   # 主页随机标语数据源（每行一条，#### 开头为注释）
├── images/                 # 图片与视频（THEME.mp4、eonun2www.png、产品图等）
├── fonts/  fronts/         # 两套字体（fronts 是历史拼写遗留）
└── static/                 # 空目录
```

### 主页（index.html）的三个核心交互
1. **视频背景**：`<video autoplay muted loop playsinline>` 播放 `images/THEME.mp4`，上层叠渐变阴影；
2. **标语轮播**：`fetch('data/trance_mixing_slogans.txt')` 按行读取、过滤注释后随机展示，加载失败时回退到默认标语；
3. **logo 动效**：eonun 图片带 CSS 呼吸流光（`softGlow` + `lightFlow` 动画），滚动时 JS 动态降低透明度/亮度/上移，回顶恢复。

---

## 4. 两种启动方式的真实链路

### 方式一：手动启动（不依赖 Hugo）✅ 当前可用
```
双击 start.html / index.html（或运行 start-server.ps1 / start-server.vbs）
        │
        ├─ start.html：纯前端导航页，点按钮跳转 public/ 下各页面
        ├─ start-server.ps1：HttpListener 伺服 public/（相对路径，可随文件夹移动）
        └─ start-server.vbs → server.ps1：同上，但 public 路径硬编码，移动文件夹即失效
```
要点：日志记录过两轮"绝对路径改相对路径"的改造，因此 **public 内页面用 file:// 双击也能基本正常**，这是手动方式可行的根本原因。

### 方式二：Hugo 部署 ❌ 当前链路不完整
```
start-server.bat → 检测 hugo → hugo server --disableLiveReload（:1313）
正式发布时：hugo 命令重新生成 public/ → 部署
```
**三个断点**（见下节）。

---

## 5. 问题清单（按严重程度排序）

### 🔴 P0：Hugo 无法重新构建出当前网站
- **根目录没有 `content/` 目录**——五个页面的正文内容全部缺失（`.trae.zip` 里也没有）；
- **`themes/ananke/` 只剩空 `layouts/`**，主题模板丢失；
- 结果：即便装好 Hugo 运行 `hugo`/`hugo server`，生成的会是近乎空白的站点，**会直接覆盖/破坏现有 public/**。

### 🟠 P1：Hugo 未安装
- 本机 `hugo` 命令不存在，`start-server.bat` 会直接报错退出。

### 🟡 P2：配置不一致
- `hugo.toml` 的 `baseURL = 'http://localhost:13736/'`，而 `start-server.bat` 提示访问 `:1313`，`README.md` 又写 `:8000`——三个端口三套说法；
- `public/index.html` 里残留 livereload 脚本和 `/index.xml` 绝对路径引用（file:// 场景下失效，属已知遗留）。

### ⚪ P3：卫生问题
- 两个 37 MB 的 zip 备份 + 27 MB `.trae.zip` 混在项目里；`server.ps1` 硬编码绝对路径；`私人非公开程序/`、`public/js`、`public/static` 空目录；`.hugo_build.lock` 残留。

---

## 6. 上线前建议路线

1. **短期（保住现有成果）**：以 `public/` 为部署对象，直接上传到任意静态托管（GitHub Pages / Netlify / Vercel / 服务器），不要运行任何 Hugo 命令；
2. **中期（恢复 Hugo 链路）**：从现网 `public/` 反向重建源码——新建 `content/`（每个页面一个 `_index.md`）、恢复完整 ananke 主题（`git submodule` 或重新下载）、把自定义样式/脚本挪进 `assets/` 或 `static/`，使 `hugo` 的输出与现有 public 逐页一致；
3. **长期**：构建验证通过前，保留 public 的只读备份（现有 zip 即可），新增内容一律走 Markdown → Hugo 的流程，不再手改 public。

---

## 7. 本次巡检记录

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 2026-09-23 | 复制项目到工作区隔离检查 | 未改动任何源文件 |
| 2026-09-23 | 通读 README、hugo.toml、维护日志、4 个启动脚本 | 启动链路已理清（§4） |
| 2026-09-23 | 扫描 public/ 全部子目录与关键 HTML 头部 | 结构确认（§3），Hugo 0.154.5 构建 |
| 2026-09-23 | 检查 .trae.zip 内容 | 同样缺少 content/ 与主题源码 |
| 2026-09-23 | 检测 Hugo 环境 | 未安装 |

*本日志由 AI 巡检生成，与 `project-maintenance-log.md`（人工日志）互补，不替代之。*

---

## 补充（2026-09-24 全部解决）

本日志第 5 节问题清单的状态更新：

- 🔴 P0（Hugo 无法重建本站）：**已解决**——content/、layouts/、static/ 已反向重建，hugo 0.166 构建验证通过；
- 🟠 P1（Hugo 未安装）：**已解决**——tools/hugo/hugo.exe 免安装二进制；
- 🟡 P2（端口不一致）：**已解决**——统一 1313（Hugo）/ 8000（静态备用），baseURL 与 bat 提示一致；
- ⚪ P3（卫生问题）：**已解决**——备份归档 backups/，旧 server.ps1 删除，空目录清除；
- 新增完成：全站资源引用排查修复、目录去编号（2-music-services→music-services、4-instructions→instructions）、局域网访问（--bind 0.0.0.0）。
详见 project-maintenance-log.md 第 11 节。
